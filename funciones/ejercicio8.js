let num1= parse.int(window.prompt("introduce un número"))

function name(params) {
    total=1
    for (let i = 1 i<=num1  i++) {
        total= total*i
        
    }
    return total; 
    console.log = total
}